{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_input.notebook_path // empty' | { read -r f; case \"$f\" in \"$CLAUDE_PROJECT_DIR\"/frontend/*) (cd \"$CLAUDE_PROJECT_DIR/frontend\" && npx --no-install prettier --write --ignore-unknown \"$f\");; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
